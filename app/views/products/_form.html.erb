<%= form_for(@product) do |f| %>
  <% if @product.errors.any? %>
    <div class="alert alert-danger" role="alert">
      <h2><%= pluralize(@product.errors.count, "error") %> prohibited this product from being saved:</h2ß>

      <ul>
      <% @product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :title %><br>
    <%= f.text_area :title, rows: 2, class: "form-control", placeholder: "Min 5 and max 60 characters.",  style: "width: 400px", autofocus: ""%>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description, rows: 6, class: "form-control", placeholder: "",  style: "width: 400px", autofocus: "" %>
  </div>
  <div class="field">
    <%= f.label :image_url %><br>
    <%= f.text_field :image_url, class: "form-control", placeholder: "*.jpg, *.gif, *.png",  style: "width: 400px", autofocus: "" %>
  </div>
  <div class="field">
    <%= f.label :price %><br>
    <%= f.text_field :price, class: "form-control", placeholder: "Price should be > 0.01$",  style: "width: 400px", autofocus: "" %>
  </div>
  <div class="actions">
    <%= f.submit 'Create product', class: "btn btn-success" %>
  </div>
<% end %>


